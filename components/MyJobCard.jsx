import React from "react";

const MyJobCard = ({ name, desc, data, handleEdit, handleDelete }) => {
  return (
    <section>
      <h1>{name} profile</h1>
    </section>
  );
};

export default MyJobCard;
